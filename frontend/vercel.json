{
  "framework": "static",
  "buildCommand": "echo 'No build needed'",
  "installCommand": "npm install express cors uuid @vercel/node",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.js" }
  ]
}